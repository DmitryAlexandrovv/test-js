function main() {
  const isMyFirstProgram = true;
  
  if (isMyFirstProgram) {
      const text = "Hello world";

      console.log(text);
  } else {
      const text = "Goodbye world";

      console.log(text);
  }
}
